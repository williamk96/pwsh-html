param (

    [Parameter(Mandatory,
    HelpMessage="Enter the text which will be inside the <h1> tag")]
    [string[]]$Text,

    # Optional parameters for HTML Global Attributes
    [Parameter(HelpMessage="Supply a single character which will be used for the accesskey attribute. \
    The accesskey attribute specifies a shortcut key to activate/focus the element.")]
    [string[]]$accesskey,
    [Parameter(HelpMessage="Supply one or more class names separates by commas. \
    The class attribute is mostly used to point to a class in a style sheet. \
    However, it can also be used by a JavaScript (via the HTML DOM) to make changes to HTML elements with a specified class.")]
    [string[]]$class,
    [Parameter(HelpMessage="Either true or false. \
    This attribute specifies whether the content of the element is editable or not. \
    Default: False")]
    [switch[]]$contenteditable,
    [Parameter(HelpMessage="Specifiy a custom data label in the format <data-some-label>. \
    The data-* attribute is used to store custom data private to the page or application.")]
    [string[]]$data,
    [Parameter(HelpMessage="The dir attribute specifies the text direction of the element's content. \
    Usage: < ltr | rtl | auto >")]
    [string[]]$dir,
    [Parameter(HelpMessage="Either true or false. \
    The draggable attribute specifies whether the element is draggable or not. \
    Default: False")]
    [switch[]]$draggable,
    [Parameter(HelpMessage="Either true or false. \
    This attribute specifies that the element is not yet, or is no longer, relevant. \
    Default: False")]
    [switch[]]$hidden,
    [Parameter(HelpMessage="Supply a unique string to identify the element.")]
    [string[]]$id,
    [Parameter(HelpMessage="Supply the language of the element's content.")]
    [string[]]$lang,
    [Parameter(HelpMessage="Either true or false. \
    Specifies whether the element is to have its spelling and grammar checked or not. \
    Default: False")]
    [string[]]$spellcheck,
    [Parameter(HelpMessage="Supply an inline CSS style for this element.")]
    [string[]]$style,
    
    [Parameter(HelpMessage="Supply the tabbing order for the element. \
    (when the 'tab' button is used for navigating)")]
    [string[]]$tabindex,
    [Parameter(HelpMessage="Supply extra information about the element. \
    The information is most often shown as a tooltip text when the mouse moves over the element.")]
    [string[]]$title,
    [Parameter(HelpMessage="Either Yes or No. \
    Specify whether the content of the element should be translated or not. \
    Default: Yes")]
    [string[]]$translate,

    # Optional parameters for HTML Window Event Attributes NOTE: for <body> tag only
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run after the document is printed.")]
    [string[]]$onafterprint,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run before the document is printed.")]
    [string[]]$onbeforeprint,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when the document is about to be unloaded.")]
    [string[]]$onbeforeunload,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when an error occurs inside the <body> tag.")]
    [string[]]$onerror,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when there has been changes to the anchor part of the URL.")]
    [string[]]$onhashchange,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run after the page is finished loading.")]
    [string[]]$onload,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when the message is triggered.")]
    [string[]]$onmessage,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when the browser starts to work offline.")]
    [string[]]$onoffline,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when the browser starts to work online.")]
    [string[]]$ononline,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when a user navigates away from a page.")]
    [string[]]$onpagehide,
    
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when a user navigates to a page.")]
    [string[]]$onpageshow,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when the window's history changes.")]
    [string[]]$onpopstate,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when the browser window is resized.")]
    [string[]]$onresize,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when a Web Storage area is updated.")]
    [string[]]$onstorage,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run once a page has unloaded (or the browser window has been closed).")]
    [string[]]$onunload,

    # Optional parameters for HTML Form Events
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run the moment that the element loses focus.")]
    [string[]]$onblur,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run the moment when the value of the element is changed.")]
    [string[]]$onchange,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when a context menu is triggered.")]
    [string[]]$oncontextmenu,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run the moment when the element gets focus.")]
    [string[]]$onfocus,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when the element gets user input.")]
    [string[]]$oninput,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when the element is invalid.")]
    [string[]]$oninvalid,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when the Reset button in a form is clicked.")]
    [string[]]$onreset,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when the user writes something in a search field (for <input="search">).")]
    [string[]]$onsearch,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run after some text has been selected in the element.")]
    [string[]]$onselect,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when a form is submitted.")]
    [string[]]$onsubmit,

    # Optional parameters for HTML Keyboard Events
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when a user is pressing a key.")]
    [string[]]$onkeydown,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when a user presses a key.")]
    [string[]]$onkeypress,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when a user releases a key.")]
    [string[]]$onkeyup,
    
    # Optional parameters for HTML Mouse Events
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when there is a mouse click on the element.")]
    [string[]]$onclick,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when there is a mouse double-click on the element.")]
    [string[]]$ondblclick,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when a mouse button is pressed down on the element.")]
    [string[]]$onmousedown,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when the mouse pointer is moving while it is over the element.")]
    [string[]]$onmousemove,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when the mouse pointer moves out of the element.")]
    [string[]]$onmouseout,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when the mouse pointer moves over the element.")]
    [string[]]$onmouseover,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when a mouse button is released over the element.")]
    [string[]]$onmouseup,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when the mouse wheel rolls up or down over the element.")]
    [string[]]$onwheel,

    # Optional parameters for HTML Drag Events
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when the element is dragged.")]
    [string[]]$ondrag,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run at the end of a drag operation.")]
    [string[]]$ondragend,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when the element has been dragged to a valid drop target.")]
    [string[]]$ondragenter,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when the element leaves a valid drop target")]
    [string[]]$ondragleave,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run")]
    [string[]]$ondragover,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run at the start of a drag operation")]
    [string[]]$ondragstart,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when the dragged element is being dropped")]
    [string[]]$ondrop,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when the element's scrollbar is being scrolled")]
    [string[]]$onscroll,

    # Optional parameters for HTML Clipboard Events
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when the user copies the content of the element")]
    [string[]]$oncopy,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when the user cuts the content of the element")]
    [string[]]$oncut,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when the user pastes some content in the element")]
    [string[]]$onpaste,
    
    # Optional parameters for HTML Media Events NOTE: Applies to all, but mainly used for <audio>, <embed>, <img>, <object>, and <video>
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run on abort")]
    [string[]]$onabort,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when a file is ready to start playing (when it has buffered enough to begin)")]
    [string[]]$oncanplay,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when a file can be played all the way to the end without pausing for buffering")]
    [string[]]$oncanplaythrough,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when the cue changes in a <track> element")]
    [string[]]$oncuechange,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when the length of the media changes")]
    [string[]]$ondurationchange,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when something bad happens and the file is suddenly unavailable (like unexpectedly disconnects)")]
    [string[]]$onemptied,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when the media has reached the end")]
    [string[]]$onended,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when the error occurs when the file is being loaded")]
    [string[]]$onerror,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when media data is loaded")]
    [string[]]$onloadeddata,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when meta data (like dimensions and duration) are loaded")]
    [string[]]$onloadedmetadata,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run just as the file begins to load before anything is actually loaded")]
    [string[]]$ononloadstart,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when the media is paused either by the user or programmatically")]
    [string[]]$onpause,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when the media is ready to start playing")]
    [string[]]$onplay,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when the media actually has started playing")]
    [string[]]$onplaying,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when the browser is in the process of getting the media data")]
    [string[]]$onprogress,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run each time the playback rate changes")]
    [string[]]$onratechange,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when the seeking attribute is set to false indicating that seeking has ended")]
    [string[]]$onseeked,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when the seeking attribute is set to true indicating that seeking is active")]
    [string[]]$onseeking,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when the browser is unable to fetch the media data for whatever reason")]
    [string[]]$onstalled,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when fetching the media data is stopped before it is completely loaded for whatever reason")]
    [string[]]$onsuspend,
    
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when the playing position has changed")]
    [string[]]$ontimeupdate,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run each time the volume is changed (which includes setting the volume to \"mute\")")]
    [string[]]$onvolumechange,
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when the media has paused but is expected to resume (like when the media pauses to buffer more data)")]
    [string[]]$onwaiting,
    # Optional parameters for HTML Misc Events
    [Parameter(HelpMessage="Supply a string of JavaScript code to be run when the user opens or closes the <details> element")]
    [string[]]$ontoggle

)